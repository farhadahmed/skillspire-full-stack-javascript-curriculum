What is the Terminal?
- A command line interface (CLI) allowing you to interact directly with the operating system (OS).
- It's faster than using the Graphic User Interface (GUI).
- The language the terminal uses is called Bash

Commands to remember
	- ls = list items 
	- cd = change directory
	- pwd = present working directory
	- .. = parent directory (one directory outside your pwd)
	- mkdir = create folder
	- touch = create file
	- / = root directory
	- ~ = home directory
	- rm = delete file
	- rm -rf = delete folder
	- mv = move/rename file or folder


Additional material (can be assigned for homework):
https://www.youtube.com/watch?v=aKRYQsKR46I&t=141s
